{% extends 'base.html.twig' %}
{% block title %} Wish create form {% endblock %}

{% block body %}
    <h1>Add your wish !</h1>
    <div>
        {{ form_start(createForm) }}
        {{ form_widget(createForm) }}
        <button type="submit" class="btn btn-primary btn-sm">Create</button>
        {{ form_end(createForm) }}
    </div>
{% endblock %}
